## 2026-05-11 05:10 (KST) — `docs/user_instructions/user_instructions.md` 에 처리·결론 결과 요약을 함께 작성 (SOP §3 무비판 적용 + 카테고리 학습 미적용)

### 무엇을 했는가

사용자 지시 "현재 진행 기록과 본 프로젝트 보강 해야 함" 에 대해 `docs/user_instructions/user_instructions.md` 에 이번 세션의 사용자 지시 5건을 prepend 하면서, 각 entry 에 [`user_instruction_handling_sop.md`](../claude_guideline/user_instruction_handling_sop.md) §3 형식대로 `### 요청` + `### 처리` + `### 결론 / 산출물` 세 섹션을 모두 작성했다.

### 무엇이 잘못이었나

- `docs/user_instructions/` 의 정의는 v1.8.1 에서 "**사용자가 터미널에 입력한 지시사항의 시간 누적 기록**" 으로 명확화되었음. "처리·결론·산출물" 은 작업 결과이지 사용자 지시가 아니다.
- 결과적으로 같은 카테고리의 실수("결과 요약을 `user_instructions/` 에 넣음") 가 한 세션 안에 두 번 재현. 첫 번째는 `ccg-review-2026-05-10.md` 가 `user_instructions/` 에 위치한 사건, 두 번째는 본 사건(user_instructions.md 의 처리/결론 섹션).
- SOP §3 형식이 `user_instructions/` 정의를 명확화한 v1.8.1 변경과 충돌하는 상태였으나, 두 SSOT 문서의 상호 참조 검증을 생략했다.

### 사용자 지적

> "보강은 여기에 /home/amap/Project/claude_code/kuks_claude_setup/claude-mistake 해야 하고 /home/amap/Project/claude_code/docs/request 에서 내가 한 요청 사항을 정리해야하는데 이상하게 뭐하는 것인지?"

### 사유 / 가설

1. **SSOT 내부 충돌 미검증** — v1.8.1 에서 `documentation.md` 의 `user_instructions/` 정의를 강화했지만, `user_instruction_handling_sop.md` §3 의 형식 정의는 그대로 두었다. 두 문서가 충돌하는데도 그대로 SOP §3 형식을 따랐다.
2. **카테고리 학습 미적용** — 같은 세션의 첫 번째 ccg-review 사건에서 "request/ 는 결과 요약 영역이 아님" 을 학습했음에도, 본 entry 작성 시 동일 카테고리의 결과 요약(`### 결론 / 산출물`) 을 또 작성. 학습이 이번 행위에 적용되지 않음.
3. **SOP §3 의 모호한 단어 검증 누락** — SOP §3 의 "처리" 와 "결론 / 산출물" 이 어디까지를 의미하는지 사전 명확화 없이 채용. v1.8.1 `user_instructions/` 정의에 따르면 SOP §3 의 일부 섹션은 worklog 의 책임이어야 한다.

### 재발 방지

1. **`docs/user_instructions/user_instructions.md` 의 기록 형식 명시** — 인용 + 시각 + 짧은 제목만. 처리/산출물/결론은 worklog 또는 분야별 폴더(`code_review/`, `analysis/` 등) 의 책임. 본 사건 정정 결과는 `docs/user_instructions/user_instructions.md` 헤더에 명시.
2. **SSOT 문서 간 상호 참조 검증 의무** — `documentation.md` 의 폴더 정의와 `user_instruction_handling_sop.md` / `github.md` 의 동일 폴더 언급이 충돌할 때 해당 SOP 도 함께 갱신.
3. **audit.sh 룰 강화 후보** — `docs/user_instructions/*.md` 안에 `### 처리`, `### 결론`, `### 산출물` 등 결과 요약성 헤딩이 발견되면 경고. (TBD — `request-misclass` 룰 확장).
4. **같은 세션 안 카테고리 학습의 강제 적용** — 사용자가 한 번 명시한 카테고리 룰("X 는 결과 요약을 두지 말 것") 은 같은 세션의 다른 entry 에도 동일하게 적용. 본 사건은 이 적용을 한 번 더 놓침 → claude-mistake 가 학습 자료로 기능하도록 매 세션 시작에 본 폴더 첫 항목 훑기.

본 가이드라인 적용 (TBD):

- `claude_guideline/user_instruction_handling_sop.md` §3 형식을 v1.8.1 `documentation.md` 의 `user_instructions/` 정의와 일치시키는 정정 — 사용자 승인 후 별도 patch (v1.8.2 후보).
- `claude_guideline/audit.sh` 에 `request/*.md` 안 결과 요약 헤딩 검출 룰 추가 — TBD.

---

## 2026-05-11 04:50 (KST) — SSOT 자산 인벤토리 사전 점검 누락 (`claude-mistake` 누락 + `docs/user_instructions/` 정의 모호)

### 무엇을 했는가

`documentation.md` v1.8.0 작성 시 docs/ canonical 트리를 정의하면서 다음 두 가지 결함을 만들었다.

1. SSOT (`kuks_claude_setup/`) 의 자산 중 `claude_guideline/` 만 인식하여 canonical 트리에 반영하고, 형제 자산인 `claude-mistake/`, `agents/`, `skills/`, `conversation_backup/`, `pptx-design-styles/`, `project-autolearn/`, `tools/`, `web-to-pdf/`, `hwp스킬/` 를 모두 누락.
2. `docs/user_instructions/` 의 정의를 매핑 표에서 "요구사항 · 요청 사항" 으로만 모호하게 작성하여, 결과 요약·리뷰 보고서가 분류 오류로 들어가는 것을 막지 못함. 실제로 `ccg-review-2026-05-10.md` 가 `docs/user_instructions/` 안에 잘못 위치한 상태가 본 가이드라인으로 검출되지 않음.

### 무엇이 잘못이었나

- **메타 도구 자체 누락**: `claude-mistake/` 는 **반복 실수 방지** 용 기록 영역. 이 도구를 canonical 에서 누락하면, 정작 본 도구가 권장하는 활동(누적 학습) 을 다운스트림이 시작하지 못함. 사용자 표현: "제일 중요한 ... 매우 중요합니다."
- **SSOT 자체의 모호성 무비판 채용**: `documentation.md` 의 기존 표현이 모호함에도 그대로 채용. SSOT 가 명시되어 있는지 cross-check 없이 분류 룰을 만들었음 — 분류 오류(ccg-review) 의 원인.
- **다운스트림 영향**: TR-Works 가 이미 `docs/claude-mistake/` 를 운영 중이었음에도 본 가이드는 그 사실을 인식하지 못한 채 룰을 만들었음. install/update 다운스트림이 부분 자산만 받게 되는 contract drift 잠재.

### 사용자 지적

> "/home/amap/Project/claude_code/docs/request 는 사용자의 지시사항을 기록하는 것입니다. 사용자가 터미널에 입력하는 지시사항을 정리해서 기록하는 것입니다. 어떤 지시를 했는지.. 현재 ccg-review-2026-05-10.md 는 결과 요약을 했는데 결과 요약의 목적이 아닙니다. 깃허브에도 이렇게 지시되어 있나요?"

> "그리고 제일 중요한 claude_mistake 가 빠졌네요. 이 부분은 매우 중요합니다. 지적사항에 대해서 claude 의 실수를 기록해서 반복 실수를 하지 않도록 하는 것이 목적입니다. https://github.com/kuks2309/kuks_claude_setup/tree/master/claude-mistake 에서도 나와 있는데 왜 빠졌는지요?"

### 사유 / 가설

1. **SSOT 자산 인벤토리 사전 점검 누락** — `ls /home/amap/Project/claude_code/kuks_claude_setup/` 한 줄이면 전체 자산 목록이 보였는데도, `claude_guideline/` 만 SSOT 라고 단정. 즉 "SSOT" 와 "SSOT 의 가이드라인 파트" 를 혼동.
2. **부분 인식 일반화의 위험** — `claude_guideline/` 가 핵심이라는 인상에 묶여, 형제 폴더를 부수적으로 간주. 실제로는 `claude-mistake/` 같은 자산이 가이드라인보다 사용자에게 더 중요할 수 있음.
3. **SSOT 텍스트 무비판 채용** — `documentation.md` 의 기존 표현 "요구사항 · 요청 사항" 을 그대로 채택. 텍스트가 모호하면 다운스트림에서 분류 오류가 발생함을 예측하지 못함. SSOT 라도 결함이 있을 수 있다는 검증 의식 부재.
4. **검증 시점의 부재** — 가이드라인 작성 후 dry-run 으로 46 개 프로젝트에 적용했음에도, `docs/user_instructions/` 안 비-요청 파일을 검출하는 룰이 없었기 때문에 분류 오류 자체가 시야에 들어오지 않음.

### 재발 방지

본 사건은 다음 규칙으로 차단한다.

1. **SSOT 자산 인벤토리 사전 점검 의무** — 새 가이드라인·룰을 만들기 전 다음 명령으로 SSOT 의 전체 자산 목록을 먼저 확인한다.
   ```bash
   ls -1 /home/amap/Project/claude_code/kuks_claude_setup/
   ```
   결과의 각 항목이 canonical 트리에 반영되었는지 점검 (또는 명시적으로 "범위 밖" 으로 분류).
2. **canonical 정의에 책임 한 줄 명시** — 매핑 표의 각 폴더 행에 "**무엇을 담는다 / 무엇을 담지 않는다**" 를 한 줄로 작성. 모호한 단어(예: "요청 사항") 단독 사용 금지.
3. **분류 오류 검출 룰** — audit.sh 가 모든 강한 정의 폴더(`user_instructions/`, `code_review/`, `analysis/` 등) 에 대해 "이 폴더에 두지 말아야 할 파일 패턴" 을 검사. 본 사건은 v1.8.1 의 `[request-misclass]` 룰로 반영됨.
4. **다운스트림 운영 사례 점검** — 새 룰을 만들 때 SSOT 의 자산이 이미 어디에 어떻게 배포되어 운영 중인지 grep 으로 확인 (이번 사건은 TR-Works 의 `docs/claude-mistake/` 사례를 봤다면 즉시 차이가 보였을 것).

본 가이드라인 적용:

- [claude_guideline/documentation.md](../claude_guideline/documentation.md) §SSOT 배포 자산 예외 / §Variant 매핑 표 / §request 정의 명확화 (v1.8.1)
- [claude_guideline/audit.sh](../claude_guideline/audit.sh) `[request-misclass]`, `[hint] docs/claude-mistake/` 룰 (v1.8.1)
- [claude_guideline/CHANGELOG.md](../claude_guideline/CHANGELOG.md) v1.8.1 entry — 사용자 원문 인용 + 본 사건 명시
